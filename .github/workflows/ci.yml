# ==============================================================================
# Continuous Integration — typecheck, build, verify Docker image
# ==============================================================================
# Runs on pull requests to master that touch the server, shared package, or deploy config.
# ==============================================================================

name: CI

on:
  pull_request:
    branches: [master]
    paths:
      - 'server/**'
      - 'shared/**'
      - 'docker-compose.prod.yml'
      - '.dockerignore'
      - '.github/workflows/ci.yml'
      - '.github/workflows/deploy.yml'

permissions:
  contents: read
  actions: write

jobs:
  ci:
    name: Server CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: |
            shared/package-lock.json
            server/package-lock.json

      - name: Install shared package
        working-directory: shared
        run: npm ci

      - name: Install server dependencies
        working-directory: server
        run: npm ci

      - name: Typecheck
        working-directory: server
        run: npm run typecheck

      - name: Build
        working-directory: server
        run: npm run build

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Verify server Docker image builds
        uses: docker/build-push-action@v7
        with:
          context: .
          file: server/Dockerfile
          push: false
          load: true
          tags: citations-server:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
