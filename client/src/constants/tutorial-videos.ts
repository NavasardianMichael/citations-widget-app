/**
 * YouTube walkthroughs for placing the widget, one per OS.
 * Empty string = not published yet; the link still renders but pressing it does nothing.
 */
export const TUTORIAL_VIDEO_URLS: Record<'android' | 'ios', string> = {
  android: '',
  ios: '',
}
