{
  "name": "citations-widget-app",
  "private": true,
  "scripts": {
    "start": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run start --prefix client\"",
    "dev": "npm start",
    "envtobase64": "npm run envtobase64 --prefix server --",
    "postinstall": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
